# Example Configuration File for KPI Executor
# Copy this file and modify the values as needed
# Usage: source example_config.env && python kpi_executor_standalone.py [args...]

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# OpenAI Configuration
export OPENAI_API_KEY="sk-proj-your-openai-key-here"

# KPI Database Configuration (where KPI definitions are stored)
export KPI_DB_HOST="localhost"
export KPI_DB_PORT="1433"
export KPI_DB_NAME="KPI_Analytics"
export KPI_DB_USER="sa"
export KPI_DB_PASSWORD="your-kpi-db-password"

# Data Database Configuration (where actual data is stored)
# If same as KPI database, you can omit these
export DATA_DB_HOST="localhost"
export DATA_DB_PORT="1433"
export DATA_DB_NAME="NewDQ"
export DATA_DB_USER="sa"
export DATA_DB_PASSWORD="your-data-db-password"

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# KPI Execution Parameters
export KG_NAME="default"
export SELECT_SCHEMA="newdqschemanov"
export LIMIT_RECORDS="1000"
export TEMPERATURE="0.0"

# Output Configuration
export OUTPUT_DIR="./results"
export VERBOSE_LOGGING="false"

# =============================================================================
# USAGE EXAMPLES
# =============================================================================

# After sourcing this file, you can run:
#
# Basic execution:
# python kpi_executor_standalone.py \
#     --openai-key "$OPENAI_API_KEY" \
#     --kg-name "$KG_NAME" \
#     --db-host "$KPI_DB_HOST" \
#     --db-user "$KPI_DB_USER" \
#     --db-password "$KPI_DB_PASSWORD" \
#     --kpi-id 19
#
# Advanced execution with separate data database:
# python kpi_executor_standalone.py \
#     --openai-key "$OPENAI_API_KEY" \
#     --temperature "$TEMPERATURE" \
#     --kg-name "$KG_NAME" \
#     --db-host "$KPI_DB_HOST" \
#     --db-port "$KPI_DB_PORT" \
#     --db-name "$KPI_DB_NAME" \
#     --db-user "$KPI_DB_USER" \
#     --db-password "$KPI_DB_PASSWORD" \
#     --data-host "$DATA_DB_HOST" \
#     --data-port "$DATA_DB_PORT" \
#     --data-db-name "$DATA_DB_NAME" \
#     --data-user "$DATA_DB_USER" \
#     --data-password "$DATA_DB_PASSWORD" \
#     --kpi-id 19 \
#     --select-schema "$SELECT_SCHEMA" \
#     --limit-records "$LIMIT_RECORDS" \
#     --output-file "$OUTPUT_DIR/kpi_19_$(date +%Y%m%d_%H%M%S).json" \
#     --verbose

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. Never commit this file with real credentials to version control
# 2. Use restrictive file permissions: chmod 600 example_config.env
# 3. Consider using a secrets management system for production
# 4. Rotate API keys and database passwords regularly
